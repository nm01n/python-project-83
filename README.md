# Page Analyzer

[![Actions Status](https://github.com/nm01n/python-project-83/workflows/Python%20CI/badge.svg)](https://github.com/nm01n/python-project-83/actions)
[![Maintainability](https://api.codeclimate.com/v1/badges/YOUR_TOKEN/maintainability)](https://codeclimate.com/github/nm01n/python-project-83/maintainability)

## Описание

Page Analyzer - веб-приложение для анализа SEO-пригодности веб-страниц. Приложение проверяет указанные страницы на наличие базовых SEO-элементов: заголовков, описаний и доступности.

### Возможности

- Добавление URL для анализа
- Проверка доступности сайта (HTTP статус-код)
- Извлечение SEO-данных:
  - Заголовок H1
  - Title страницы
  - Meta-описание (description)
- История всех проверок
- Хранение данных в PostgreSQL

## Демо

🔗 [Посмотреть проект](https://page-analyzer-2895.onrender.com)

## Технологии

- **Backend:** Python 3.10+, Flask 3.0
- **База данных:** PostgreSQL
- **Парсинг:** BeautifulSoup4, Requests
- **Frontend:** Bootstrap 5
- **Deployment:** Render.com (PaaS)
- **WSGI сервер:** Gunicorn
- **Линтер:** Ruff

## Требования

- Python 3.10 или выше
- PostgreSQL 12 или выше
- uv (пакетный менеджер)

## Установка

### 1. Клонируйте репозиторий
```bash
git clone https://github.com/nm01n/python-project-83.git
cd python-project-83
```

### 2. Установите зависимости
```bash
# Установите uv (если еще не установлен)
curl -LsSf https://astral.sh/uv/install.sh | sh

# Установите зависимости проекта
make install
```

### 3. Настройте базу данных
```bash
# Создайте базу данных PostgreSQL
sudo -u postgres createdb page_analyzer

# Примените миграции
sudo -u postgres psql -d page_analyzer -f database.sql
```

### 4. Настройте переменные окружения

Создайте файл `.env` в корне проекта:
```bash
SECRET_KEY=your-secret-key-here
DATABASE_URL=postgresql://postgres@localhost:5432/page_analyzer
```

## Запуск

### Режим разработки
```bash
make dev
```

Приложение будет доступно по адресу: http://localhost:5000

### Режим продакшена
```bash
make start
```

Приложение будет доступно по адресу: http://localhost:8000

## Команды разработки
```bash
# Установка зависимостей
make install

# Запуск в режиме разработки
make dev

# Запуск в режиме продакшена
make start

# Проверка кода линтером
make lint

# Автоматическое исправление ошибок
make fix

# Форматирование кода
make format
```

## Структура проекта
```
python-project-83/
├── page_analyzer/          # Основной пакет приложения
│   ├── __init__.py
│   ├── app.py             # Flask приложение и маршруты
│   └── templates/         # HTML шаблоны
│       ├── base.html
│       ├── index.html
│       ├── urls.html
│       └── url.html
├── database.sql           # SQL схема базы данных
├── pyproject.toml         # Конфигурация проекта и зависимости
├── Makefile              # Команды для разработки
├── build.sh              # Скрипт сборки для деплоя
└── README.md
```

## Деплой на Render.com

1. Создайте PostgreSQL базу данных на Render.com
2. Создайте Web Service, подключите репозиторий
3. Укажите команды:
   - **Build Command:** `make build`
   - **Start Command:** `make render-start`
4. Добавьте переменные окружения:
   - `SECRET_KEY`
   - `DATABASE_URL`

## Лицензия

Этот проект создан в образовательных целях в рамках обучения на [Hexlet](https://hexlet.io).

## Автор

Ольга (https://github.com/nm01n)
